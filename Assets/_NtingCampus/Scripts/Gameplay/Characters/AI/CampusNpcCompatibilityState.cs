namespace NtingCampus.Gameplay.Characters
{
    // Legacy compatibility stub kept only to preserve enum slot references during migration.
    public enum CampusNpcCompatibilityState
    {
        None = 0
    }
}
