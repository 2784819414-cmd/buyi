This folder contains the old inventory grid MVP.

The active storage UI direction is `Assets/_Nting/UI/Storage`.

Do not build new gameplay features on this folder.

Keep only for reference until safely removed.
